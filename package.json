{
  "name": "sistema-inventario",
  "version": "1.0.0",
  "description": "Sistema web de inventário com leitura de QR codes e controle de estoque",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "init-db": "node src/database/init.js",
    "build": "npm run init-db",
    "railway:start": "npm run build && npm start",
    "docker:build": "docker build -t sistema-inventario .",
    "docker:run": "docker run -p 3000:3000 sistema-inventario",
    "docker:dev": "docker-compose --profile dev up",
    "docker:prod": "docker-compose up",
    "test": "echo \"No tests specified\" && exit 0",
    "postinstall": "npm run init-db"
  },
  "keywords": [
    "inventory",
    "qr-code",
    "stock",
    "express",
    "sqlite"
  ],
  "author": "Hendel Santos",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "moment": "^2.29.4",
    "multer": "^1.4.5-lts.1",
    "sqlite3": "^5.1.6",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "dotenv": "^16.6.1",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
